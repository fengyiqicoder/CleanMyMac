MODULE_NAME="Cursor IDE caches"
MODULE_DESCRIPTION="Cursor editor on-disk caches (mirrors VS Code structure). User settings preserved."
MODULE_RISK="low"
MODULE_CATEGORY="dev"
MODULE_PATHS=("$HOME/.cursor/Cache" "$HOME/.cursor/CachedData" "$HOME/.cursor/Code Cache" "$HOME/.cursor/GPUCache" "$HOME/.cursor/logs")
